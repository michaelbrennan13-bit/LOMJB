# Decisions Log

Michael's corrections and preferences, in the order he gave them. This is August's long-term memory: the `retro` skill appends here, and every entry that changes behavior also gets applied to the relevant skill, `firm/voice.md`, or `CLAUDE.md`.

**Append only. Never overwrite.** The history of how the firm's preferences evolved is itself useful.

## Format

```
## <yyyy-mm-dd> — <short title>
**Correction:** <what Michael changed or asked for>
**Rule going forward:** <the durable rule, stated so a future session can apply it>
**Applied to:** <file(s) edited — or "log only">
```

---

## 2026-08-05 — Scaffold built
**Correction:** Initial build of August v1 per Michael's build prompt.
**Rule going forward:** Operate per `CLAUDE.md`: three autonomy tiers, absolute citation discipline (`[VERIFIED]` / `[VERIFY]`), confidentiality (genericize web queries), flag-don't-refuse ethics posture, and grow via this log + the `retro` skill.
**Applied to:** entire repo (v1 scaffold).

<!-- New entries go below this line, newest last. -->
